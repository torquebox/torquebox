---
application:
  root: ${basedir}/apps/rack/basic
  env: development
web:
  context: /vhosting
  host: integ-app1.torquebox.org
environment:
  GRIST: this-is-app-one

ruby:
  version: ${ruby.compat.version}
