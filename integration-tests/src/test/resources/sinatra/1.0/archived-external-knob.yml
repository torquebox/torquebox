application:
  root: ${basedir}/apps/sinatra/1.0/override.knob
  env: development
web:
  rackup: external.ru
  static: external.static
  context: /override-external
environment:
  foot: stink
  bar:  maid
