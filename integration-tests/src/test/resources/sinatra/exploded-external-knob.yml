application:
  root: ${basedir}/apps/sinatra/override
  env: development
web:
  rackup: external.ru
  static: external.static
  context: /override-external
environment:
  foot: stink
  bar:  maid

ruby:
  version: ${ruby.compat.version}
